# Diário · 17 a 19 de setembro de 2026

> Movido **íntegro** do `CLAUDE.md` em 20/09/2026, card `navyr-deploy#47`.
> Nada foi cortado, reescrito ou resumido — só mudou de arquivo.
>
> **Isto é histórico, não estado.** Os números aqui eram verdade nas datas
> citadas e não são conferíveis hoje. Para o estado atual, o quadro:
> https://github.com/orgs/navyr-io/projects/1
>
> Este bloco vivia no arquivo lido a cada sessão e **afirmava que o CI estava
> morto**. Ninguém conferiu por duas semanas, porque a documentação dizia que
> não valia a pena. É a razão de o `CLAUDE.md` ter passado a separar DURÁVEL,
> DERIVADO e HISTÓRICO.
>
> As lições que valem para sempre não ficaram aqui: foram promovidas para as
> *Armadilhas de ambiente* do `CLAUDE.md`.

---

## Estado em 17/09/2026

Quadro em **187 itens**: 170 Concluído, 3 Aberto, 2 Aguardando decisão, 2 Em
andamento, 1 Bloqueado, 9 Despriorizado — consultado ao vivo em 17/09. O
`backlog.md` está congelado em 20/08 e **não serve como fonte de estado**; o
quadro serve.

| Status | Itens |
|---|---|
| Aberto | `#33` token do job de contrato · `#35` Playwright instável |
| Em andamento | `#17` ECS — contrato pronto, falta o Terraform |
| Aguardando decisão | `#4` GitHub Team |
| Bloqueado | `#1` branch protection nos 11 privados, por `#4` |
| Despriorizado | `#7` motor de labs · `#8` licenciamento |

### Delta de 19/09/2026

Quadro consultado ao vivo: **166 itens** — 149 Concluído, 14 Aberto, 2
Despriorizado, 1 Em andamento. O número acima (187) era de outra leitura e não
bate; o quadro é a fonte, este arquivo é histórico.

**O Frontend V2 saiu de Despriorizado.** `#26` (marco) e `#27`–`#32` (fases A–F)
foram movidos para `Aberto` por decisão do Erick. O trabalho vive em
`navyr-frontend-v2`, privado, com `DESIGN.md` (17 seções, convertido do `.docx`
que só existia no Downloads de uma máquina Windows) e a **SPEC-037**, que traduz
o design em 19 ciclos com critério de pronto verificável.

**A SPEC-037 não fica no `navyr-docs`.** Ele é público, e o documento descreve o
produto tela a tela. A numeração de spec é global na frota; o repositório varia
conforme o conteúdo poder ou não ser público. A skill `navyr-spec` registra a
exceção.

**As SPEC-030 a 033 nunca tinham sido commitadas** — existiam só neste disco,
como os portões antes do `#34`. Commitadas em 19/09, após varredura por padrão de
credencial.

**A execução da V2 é delegada ao Codex, com coordenação externa.** Quem coordena
lê o quadro, escolhe o card, escreve o briefing do ciclo, verifica rodando os
gates e move o Status; quem executa recebe um ciclo por vez e devolve evidência
colada. O `AGENTS.md` do `navyr-frontend-v2` é o contrato dessa divisão.

**Bloqueio aberto em 19/09:** o `codex-cli` local é **0.121.0** contra **0.155.1**
no npm, e não consegue ler o catálogo de modelos do servidor (`unknown variant
'max'`). Nenhum modelo testado roda com a auth `chatgpt` — `gpt-5.4`,
`gpt-5.4-codex`, `gpt-5-codex`, `gpt-5.3-codex` e `codex-mini-latest` devolvem
`400 · not supported`. Até o upgrade, não há despacho.

**Fecharam em 17/09:** `#3` (v0.1.0 completa), `#12` (terceiro instala),
`#18`, `#19`, `#20`, `#21`, `#22`, `#23`, `#24`, `#25`, `#34`.

**Uma única decisão trava o resto: o `#4`.** Assinar ou não o GitHub Team é o que
falta para `#1` nos 11 repositórios privados. O `#33` precisa de um PAT. O `#17`
precisa de uma escolha sobre cache do Trivy antes do Terraform.

**A visibilidade foi decidida e NÃO foi abrir tudo.** O `#12` criou o
`navyr-install`, público, com só os artefatos de instalação — porque o
`navyr-deploy` carrega 35 issues, 12 descrevendo achados de segurança em aberto.
O `#21` manteve o `.github` privado, duplicando 40 linhas de `release.yml` no
`navyr-docs`. Consequência assumida: branch protection continua custando nos
privados.

**O CI voltou a rodar em 01/09**, quando o teto de minutos resetou no ciclo de
cobrança. Em 16/09 o consumo era de **146 de 2000 minutos**, com reset em 01/10.

Esse número não é mais conferível pela API: o endpoint
`/orgs/{org}/settings/billing/actions` responde **410 — moved** desde a mudança
de billing do GitHub. E 17/09 consumiu bem mais que os dias anteriores — 9
pushes com CI, 7 `workflow_dispatch` e 11 releases com build multi-arch. Para
saber o consumo real hoje, a fonte é a tela de billing, não a API.

A prova está na duração dos jobs, não na ausência de erro: durante o bloqueio
eles morriam em 2–4s sem executar passo nenhum (`navyr-auth` run 33432615584 —
`started 19:48:54 → completed 19:48:56`); depois do reset há runs de 244s, 454s
e 597s com passos reais.

**O gate ficou verde em 16/09** (cards `#18` e `#19`) e **endureceu em 17/09**.
Os **7 serviços Go** têm run verde no SHA exato da `main` — conferido um a um,
não pelo último run do repositório, que pode ser um `cancelled` do
`cancel-in-progress`.

Duas exceções, ambas com card:

- `navyr-deploy`: o job `contrato` reprova desde 30/08 por falta de
  `NAVYR_REPOS_TOKEN` (`#33`). O job `compose` passa, e os três testes de
  contrato passam localmente — o defeito é de acesso, não dos testes.
- `navyr-frontend`: a suíte Playwright é **instável** (`#35`). Em 17/09, no
  mesmo código de aplicação: `c668d02` reprovou, `6de9d0c` passou com 21 de 22
  em 1,5 min, `989d877` reprovou com 3 falhas em 5,8 min. As três falhas
  estouram `locator.click` de 30s no mesmo helper de login.

**A v0.1.0 está marcada.** 11 tags criadas, 10 releases publicadas, **8 de 8**
imagens versionadas no GHCR. Falta só o `navyr-docs`, bloqueado pelo `#21` — e
não por defeito: repositório público não alcança workflow reutilizável de
repositório privado, e o `.github` é privado.

**A fila do Dependabot foi drenada nos serviços Go.** Dos 28 PRs parados, 19
mergeados e 1 fechado pelo próprio Dependabot por ter virado redundante
(`navyr-gateway#5`: o merge do `#4` já trouxera o `aws-sdk-go-v2` base por
transitividade). Restam 9 — 7 do `navyr-frontend` e 2 do `navyr-helm` —
adiados por decisão, dois deles majors (`node 22→26`, `tailwind 3→4`).

Os 7 serviços Go convergiram para `golang:1.27.1-alpine`, e o `navyr-agent`
está em `k8s 0.37.0` com a migração para `k8s.io/streaming`.

**O verde daqueles PRs não valia nada, e essa é a lição da drenagem.** O que o
painel de checks mostrava era resultado de execuções de agosto e 01/09, contra
uma `main` que não existe mais e um gate que mudou. O vermelho de seis deles era
o `#18` e o `#19`, não defeito neles: rebaseados, passaram. Cada merge exigiu
rebase e execução nova; e nos repositórios que receberam vários bumps — `auth` 3,
`gateway` 4, `orchestrator` 5 — o verde individual de cada PR precede o merge dos
demais, então o estado COMBINADO foi verificado por `workflow_dispatch` na `main`
ao fim de cada leva.

Bumps de AWS SDK no mesmo repositório disputam `go.mod` e `go.sum`: cada merge
invalida os irmãos. Não há como fazer em lote — é merge, rebase dos irmãos,
esperar, repetir. No `orchestrator` foram três rodadas.

**O que 17/09 mudou no gate:**

- `G702` **religada** (`#23`). A regra estava fora por uma afirmação falsa sobre
  `#nosec`. Medição: 3 achados na frota, todos num arquivo, todos resolvidos por
  anotação. Hoje `-exclude=G118` e `Issues: 0` nos 7.
- `workflow_dispatch` em todos os `ci.yml` (`#24`). Antes não existia forma de
  pedir uma execução do gate sem empurrar código — `navyr-agent`, `navyr-billing`
  e `navyr-collector` passaram de 19/08 a 17/09 sem evidência nenhuma.
- Portão local do `govulncheck` alinhado ao CI (`#25`), e agora **bloqueante**:
  reprova só em alcançável com correção disponível, que é o critério do CI.
- `arm64` do frontend de volta (`#22`), pela fixação em `$BUILDPLATFORM`.
- **Portões versionados** (`#34`): `navyr-deploy/scripts/estacao/`. Antes viviam
  numa única máquina, fora de repositório. O `core.hooksPath` global aponta para
  lá e o `CLAUDE.md` da raiz é **link simbólico** — cópia viraria descrição
  divergente. Ao mover, os 11 repos ficaram com `hooksPath` local apontando para
  diretório inexistente, e **o git ignora isso em silêncio**: por minutos, todo
  push passaria sem portão. Config local removida nos 11.
- **Contrato da plataforma** (`#17`): `navyr-deploy/contrato/plataforma.yaml`,
  gerado das fontes e commitado — 9 serviços, 72 variáveis, e quais têm estado
  com o gerenciado que os substitui em nuvem. Os empacotamentos passam a ser
  CONSUMIDORES verificados contra ele, em vez de cada um re-derivar. É a
  fundação que o caminho ECS exigia. A catraca foi verificada MORDENDO em três
  perturbações, não só passando.

Isso ficou invisível por duas semanas porque **esta seção dizia que o CI estava
morto** — a documentação afirmava que não valia a pena conferir, e ninguém
conferiu. É a razão de ela existir com data no título.

**Os 7 achados do `gosec` eram falsos positivos, e a correção não foi nenhuma
das quatro opções que o `#18` listava.** A premissa do card era falsa: `#nosec`
FUNCIONA em regra de taint — só precisa estar na linha que o gosec **reporta**
(o sink), não na que parece a origem. Bastou anotar o sink que faltava. Não se
desligou regra, não se criou baseline, não se subiu versão, não se refatorou 54
pontos.

O `#19` eram três defeitos independentes, todos reais e localizados:
`effectiveScopeFromTokenClaims` órfã desde o `2772182` — e era o caminho VELHO,
o que dava 403 "missing claims" para operator e viewer; `QF1001` caindo em
`rotaDeAuth.casa`, anotado com o motivo em vez de reescrever booleano de bypass
de auth; e um teste de integração de 19/08 que a migration `000030` (27/08)
tornou falso, gravando `ready` numa coluna que passou a aceitar só ciclo de vida.

**`navyr-agent`, `navyr-billing` e `navyr-collector` seguem sem run verde em
`main`** — e não por defeito: os três estão verdes localmente. É que ninguém
empurrou nada para eles desde o apagão, então o último run é o de agosto, morto
pela cota em 3–4s. Falta o push (ou um `workflow_dispatch`) que produza a
evidência.

**28 PRs do Dependabot** parados, e o que os segurava saiu do caminho. O
`SA1019` do `navyr-agent` foi resolvido em 17/09 (`#20`): não era troca de
import — o pacote novo devolve um tipo que o `Upgrader` do client-go ainda não
aceita, e a saída foi `NewUpgraderForStreaming`, ponte que o próprio client-go
0.37 fornece. O prazo do `#14` continua valendo porque ele nunca viveu no
pacote: vive no `DialContext`.

**Continua bloqueado por plano:** branch protection responde `403 — Upgrade` em
repo privado no Free. É parede de plano, não trabalho pendente (`#1`, `#3`,
`#4`).
